let words = ["hello", "world"];

function getWordLengths(words) {
  return words.map((numword) => numword.length);
  // Start coding here
}

const result = getWordLengths(words);

console.log(result); // Output: [5, 5]
